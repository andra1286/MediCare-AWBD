spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:medicare_identity}
    username: ${DB_USER:medicare}
    password: ${DB_PASSWORD:medicare}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
