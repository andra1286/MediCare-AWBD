spring:
  application:
    name: web-ui

server:
  port: 8090
  error:
    whitelabel:
      enabled: false

# Backend service URLs (direct in dev; point at the gateway in full deployment).
medicare:
  appointment:
    url: ${APPOINTMENT_URL:http://localhost:8082}
  records:
    url: ${RECORDS_URL:http://localhost:8083}

management:
  endpoints:
    web:
      exposure:
        include: health,info
